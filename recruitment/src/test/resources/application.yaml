
spring:
  # DataSource
  datasource:
    url: jdbc:postgresql://192.168.35.221:5432/recruitment_test
    username: db
    password: postgres

  # JPA
  jpa:
    hibernate:
      ddl-auto: create # 도메인 모두 설계 후에는 update, false
      show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC